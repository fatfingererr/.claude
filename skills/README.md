# Claude Code Skills 使用指南

本目錄包含所有可用的 Skills，提供專業的領域知識與工作流程指導

## Skills 概覽

Skills 是模組化的領域專業知識系統，透過 `/<skill 名稱>` 指令即可啟動。

每個 skill 都包含特定領域的最佳實踐、工作流程、參考資料與模板。

## 工具開發 Skills

### 1. create-agent-skills
**用途：** 建立、撰寫、優化 Claude Code Skills

**何時使用：**
- 想建立新的 skill
- 改進現有 skill 的結構
- 了解 skill 的最佳實踐
- 設計 workflows、references、templates 架構

**啟動指令：**
```bash
/create-agent-skill
```

**核心功能：**
- 教你如何建立有效的 SKILL.md
- Router pattern 設計指導
- Pure XML 結構規範
- Skills 的檔案組織最佳實踐

**適合場景：**
- 「我想建立一個新的 skill 來處理 X 任務」
- 「這個 skill 的結構可以如何改進？」
- 「如何組織 workflows 和 references？」

---

### 2. create-hooks
**用途：** 建立、設定、使用 Claude Code hooks

**何時使用：**
- 設定事件監聽器（PreToolUse, PostToolUse, Stop 等）
- 驗證指令執行
- 自動化工作流程
- 新增通知功能
- 實作自訂完成條件

**啟動指令：**
```bash
/create-hook
```

**核心功能：**
- Hooks 的事件驅動架構
- 各種 hook 類型說明（SessionStart, UserPromptSubmit 等）
- Shell 指令或 LLM prompt 整合
- Hook 除錯技巧

**適合場景：**
- 「我想在每次執行指令前先驗證某些條件」
- 「如何在 Claude 使用某個工具時自動執行腳本？」
- 「怎麼建立專案特定的自動化流程？」

---

### 3. create-slash-commands
**用途：** 建立自訂 slash commands

**何時使用：**
- 建立可重複使用的 prompt 範本
- 標準化團隊工作流程
- 使用 YAML frontmatter 設定指令
- 處理動態參數與 context 載入

**啟動指令：**
```bash
/create-slash-command
```

**核心功能：**
- Slash command 的 YAML 結構
- XML 標籤使用規範
- 參數處理與驗證
- 動態 context 注入

**適合場景：**
- 「我想建立一個 /review-code 指令」
- 「如何讓指令接受參數？」
- 「怎麼在指令中載入專案特定的 context？」

---

### 4. create-subagents
**用途：** 建立、設定、使用 Claude Code subagents

**何時使用：**
- 委派複雜任務給專門的 agent
- 建立隔離的執行環境
- 設定 agent 的工具存取權限
- 編排多 agent 工作流程

**啟動指令：**
```bash
/create-subagent
```

**核心功能：**
- Subagent 系統架構
- 強大的 system prompt 撰寫
- 工具存取控制
- Task tool 的使用方式
- 多 agent 協作模式

**適合場景：**
- 「我想建立一個專門做 code review 的 agent」
- 「如何限制 agent 只能使用特定工具？」
- 「怎麼讓多個 agent 協同工作？」

---

### 5. create-meta-prompts
**用途：** 建立 Claude-to-Claude pipeline 的優化 prompts

**何時使用：**
- 設計多階段工作流程（研究 → 規劃 → 實作）
- 建立 prompt 產生其他 prompts 使用的輸出
- 優化 Claude 之間的資訊傳遞
- 產生結構化的 XML 與 metadata

**啟動指令：**
```bash
/create-meta-prompt
```

**核心功能：**
- Claude-to-Claude 溝通最佳化
- 結構化輸出設計（XML + metadata）
- 自動產生 SUMMARY.md
- Pipeline 階段設計

**適合場景：**
- 「我想建立一個『研究 → 分析 → 建議』的 pipeline」
- 「如何讓一個 prompt 的輸出變成下一個 prompt 的輸入？」
- 「怎麼設計有效的多階段工作流程？」

---

### 6. create-plans
**用途：** 建立分層式專案計畫（為獨立開發者 + Claude 優化）

**何時使用：**
- 規劃專案、階段、任務
- 建立 Claude 可執行的計畫
- 設計驗證標準
- 處理 briefs、roadmaps、phase plans

**啟動指令：**
```bash
/create-plan
```

**核心功能：**
- 獨立開發者 + Claude 的規劃模式（非企業文件）
- 分層計畫架構
- Claude 可執行的規格
- Context handoff 機制

**適合場景：**
- 「我想規劃一個新專案的實作階段」
- 「如何建立 Claude 能理解並執行的計畫？」
- 「怎麼設計階段性的開發藍圖？」

## 除錯分析 Skills

### 7. debug-like-expert
**用途：** 深度分析除錯模式，處理複雜問題

**何時使用：**
- 標準除錯方法失敗時
- 需要系統化的根因分析
- 複雜問題需要科學方法論
- 要避免認知偏差（特別是對自己寫的程式碼）

**啟動指令：**
```bash
/debug
```

**核心功能：**
- 方法論調查協定
- 證據蒐集技巧
- 假設測試框架
- 嚴格驗證流程
- 科學方法應用

**適合場景：**
- 「這個 bug 試了很多方法都找不到原因」
- 「系統行為與預期不符，需要深入分析」
- 「我對自己的程式碼有盲點，需要客觀檢視」

**核心理念：**
> 「對你寫的程式碼要比陌生程式碼更加懷疑，因為認知偏差會讓你看不見實際的實作錯誤。」

## 🚀 快速開始

### 如何使用 Skill？

#### 方法 1：直接指令
```bash
/create-agent-skill
/create-hook
/create-slash-command
/create-subagent
/create-meta-prompt
/create-plan
```

#### 方法 2：在對話中直接提到需求
Claude 會自動判斷是否需要啟動相關 skill。

**範例：**
- 「我想建立一個新的 skill」 → 自動啟動 create-agent-skills
- 「這個 bug 很奇怪，試了很多方法都找不到」 → 建議使用 debug-like-expert
- 「幫我設計一個專案計畫」 → 可能啟動 create-plans


## Skill 結構說明

每個 skill 通常包含以下結構：

```
skill-name/
├── SKILL.md              # 主要定義檔（核心指令）
├── README.md             # 使用說明（可選）
│
├── references/           # 參考資料（知識庫）
│   ├── concept-A.md
│   └── concept-B.md
│
├── templates/            # 輸出模板
│   ├── template-1.md
│   └── template-2.md
│
└── workflows/            # 工作流程
    ├── workflow-A.md
    └── workflow-B.md
```

### 資料夾用途

- **SKILL.md**：skill 的核心定義，Claude 啟動 skill 時一定會讀取
- **workflows/**：多步驟流程，Claude 會「遵循」這些步驟
- **references/**：領域知識，Claude 會「閱讀」來獲取 context
- **templates/**：一致的輸出結構，Claude 會「複製並填充」
- **scripts/**：可執行的程式碼，Claude 會「直接執行」


## 使用技巧

### 1. 何時使用哪個 Skill？

| 你想做什麼             | 使用這個 Skill        |
|------------------------|-----------------------|
| 建立新的 skill         | create-agent-skills   |
| 設定事件觸發的自動化   | create-hooks          |
| 建立 `/command` 指令   | create-slash-commands |
| 建立專門的 AI agent    | create-subagents      |
| 設計多階段 AI 工作流程 | create-meta-prompts   |
| 規劃專案開發計畫       | create-plans          |
| 深度除錯複雜問題       | debug-like-expert     |

### 2. Skills 可以組合使用

**範例：建立完整的自動化工作流程**
1. 用 `create-plans` 規劃專案
2. 用 `create-subagents` 建立執行 agent
3. 用 `create-hooks` 設定自動化觸發
4. 用 `debug-like-expert` 除錯問題

### 3. 自訂與擴充

所有 skills 都可以：
- 修改 SKILL.md 調整行為
- 新增 references/ 擴充知識庫
- 建立 templates/ 標準化輸出
- 設計 workflows/ 流程化操作


## 進階使用

### 建立你自己的 Skill

使用 `create-agent-skills` 學習如何建立：

```bash
/create-agent-skill
```

然後告訴它你想建立什麼樣的 skill。

### Skill 最佳實踐

1. **SKILL.md 保持簡潔**：只放核心原則
2. **善用 Router Pattern**：複雜 skill 用 workflows/ 分流
3. **Pure XML 結構**：不用 markdown heading (#)
4. **提供範例**：每個概念都給具體範例
5. **可執行性**：確保輸出可以立即使用


## 常見問題

### Q: 如何知道有哪些 skills 可用？

A: 檢查 `.claude/skills/` 目錄，或直接問 Claude：
```
「有哪些 skills 可以使用？」
```

### Q: Skill 和 Slash Command 有什麼不同？

A:
- **Skill**：完整的領域知識系統，包含 workflows、references、templates
- **Slash Command**：單一用途的快速指令，通常是一個 prompt 範本

Skill 更適合複雜的、需要多步驟的任務；Slash Command 適合快速、重複性的操作。

### Q: 可以修改現有的 skill 嗎？

A: 可以！所有 skills 都是檔案形式，你可以：
- 修改 SKILL.md 調整行為
- 新增 references/ 擴充知識
- 建立 templates/ 標準化輸出
- 設計 workflows/ 客製化流程

### Q: 如何讓 Claude 自動選擇正確的 skill？

A: Claude 會根據對話內容自動判斷。你也可以：
- 明確提到 skill 名稱（例如：「用 debug-like-expert 幫我分析」）
- 使用 `/skill-name` 指令
- 描述你的需求，讓 Claude 推薦適合的 skill

### Q: Expertise 目錄下的 skills 怎麼使用？

A: Expertise skills 通常是被動式的知識庫，Claude 會在需要時自動參考。例如：
- 當你問 iPhone 開發問題時，會自動參考 `expertise/iphone-apps/`
- 當你問 macOS 開發問題時，會自動參考 `expertise/macos-apps/`

---

## 延伸學習

### 想深入了解某個 Skill？

每個 skill 目錄下可能包含：
- `README.md`：詳細使用說明
- `references/`：深度參考資料
- `workflows/`：完整工作流程
- `templates/`：可複製的模板

**範例：**
- `create-agent-skills/references/` - Skill 設計參考資料
- `create-plans/workflows/` - 專案規劃工作流程


## 下一步

1. **探索感興趣的 Skill**
   - 瀏覽上述 skills 清單
   - 閱讀各 skill 的 README.md（如果有）

2. **實際使用 Skill**
   - 選擇一個與你當前任務相關的 skill
   - 使用 `/skill-name` 啟動
   - 跟著 Claude 的引導操作

3. **建立自己的 Skill**
   - 使用 `/create-agent-skill`
   - 為你的專案建立客製化的 skill

4. **組合多個 Skills**
   - 嘗試在一個專案中使用多個 skills
   - 建立完整的工作流程

## 獲取幫助

如果你：
- 不確定該用哪個 skill
- 想了解某個 skill 的詳細功能
- 需要客製化某個 skill

直接在對話中詢問 Claude！例如：
```
「我想建立一個自動化測試的工作流程，應該用哪些 skills？」
「create-hooks 和 create-slash-commands 有什麼不同？」
```

**最後更新：** 2025-12-26
